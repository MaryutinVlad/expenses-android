import { StyleSheet } from "react-native";

const fonts = StyleSheet.create({
  username: {
    fontSize: 30,
  },
  titleOnButton: {
    fontSize: 22,
    paddingTop: 3,
  },
  stdHeader: {
    fontSize: 20,
  },
  bigHeader: {
    fontSize: 23,
  },
});

export default fonts;